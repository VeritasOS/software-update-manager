Description=Check if backed up copy of appliance node version info exists...
# Don't hardcode absolute path if it's inside plugin dir, instead use $PM_LIBRARY as shown below:
ExecStart=/bin/sh $PM_LIBRARY/version/rollback-precheck.sh
