Description=Checking appliance update status...
# Don't hardcode absolute path if it's inside plugin dir, instead use $PM_LIBRARY as shown below:
ExecStart=/bin/sh $PM_LIBRARY/version/install-precheck.sh
